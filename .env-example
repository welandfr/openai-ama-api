FLASK_APP=app
FLASK_ENV=development

# KEY FOR OpenAI (ChatGPT)
OPENAI_API_KEY=<API KEY from https://platform.openai.com/account/api-keys>

# Main key for accessing this app (unlimited tokens)
API_KEY=my-very-secret-key 

# Daily token limit for users (resets on app start)
TOKEN_LIMIT=1000

# Comma-separated list of user keys (no spaces)
USER_KEYS=user1k3y,user2k3y